databases:
  - name: dashbord_backend
    databaseName: dashbord_backend
    user: dashbord_backend

services:
  - type: web
    name: dashbord_backend
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dashbord_backend
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false